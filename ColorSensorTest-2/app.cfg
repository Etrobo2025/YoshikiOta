INCLUDE("app.h");  // main_task の宣言を読み込む

// メインタスクの生成
CRE_TSK(MAIN_TASK, {
    TA_ACT,        // タスク自動起動（起動時に自動で実行される）
    1,             // タスクの優先度（数字が小さいほど高優先）
    main_task,     // タスクのエントリポイント（関数名）
    4096,          // スタックサイズ（単位: バイト）
    NULL           // 拡張情報（intptr_t 型の引数、未使用なら NULL）
});
